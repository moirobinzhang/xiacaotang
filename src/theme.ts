import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#fdfaf5',
    },
    primary: {
      main: '#1a2f23',
    },
  },
});

export default theme;